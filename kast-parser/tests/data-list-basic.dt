data List (a : Set 0) : Set 0 where
  Nil : List a,
  Cons : Pi x a (Pi y (List a) (List a))

data Nat : Set 0 where
  Zero : Nat,
  Succ : Pi x Nat Nat


expr = ((Lambda x (List Nat) (Cons Nat Zero x)) (Cons Nat Zero (Cons Nat (Succ
Zero) (Nil Nat))))
