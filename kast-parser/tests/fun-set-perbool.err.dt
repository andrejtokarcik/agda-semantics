data Bool : Set where
  True : Bool,
  False : Bool
;
data T : Set 1 where
  E : T

// s cannot be typed since we don't have a cummulative hierarchy!
; s True = Set 0
; s False = Set 1
; f = Lambda x (s True) x
; g = f Bool
; h = f T
